[workspace]
members = ["ast-builder", "engine", "fuzz/bytes", "fuzz/map-keys", "wasm"]

default-members = ["ast-builder", "engine"]

[profile.release]
panic = "unwind"
lto = true

[profile.dev]
panic = "unwind"
